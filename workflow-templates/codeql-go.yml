name: CodeQL (Go)

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 5 * * 1"

permissions: read-all

jobs:
  codeql:
    uses: vhco-pro/.github/.github/workflows/reusable-codeql-go.yml@main
